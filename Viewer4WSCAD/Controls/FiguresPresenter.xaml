<UserControl x:Class="Viewer4WSCAD.Controls.FiguresPresenter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Viewer4WSCAD.Controls"
             mc:Ignorable="d" x:Name="MyFiguresPresenter"
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ItemsControl ItemsSource="{Binding ElementName=MyFiguresPresenter, Path=Figures}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        <Canvas Grid.Column="1" x:Name="MyCanvas">
            <Canvas.Children>
                <Polygon Fill="Black">
                    <Polygon.Points>
                        <Point X="10" Y="10"/>
                        <Point X="100" Y="10"/>
                        <Point X="100" Y="100"/>
                    </Polygon.Points>
                </Polygon>
            </Canvas.Children>
        </Canvas>
    </Grid>
</UserControl>
